const HelpButton = () => {
    return (
        <div>
            <button type="button" className="w-full px-4 py-2 text-white font-semibold rounded-md shadow-md bg-transparent border-2 border-white hover:bg-white hover:text-nutmeg transition duration-300 ease-in-out">
                ?
            </button>
        </div>
    );
};

export default HelpButton;